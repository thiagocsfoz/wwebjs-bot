version: '3.8'

services:
  node19:
    build:
      network: host
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://127.0.0.1:27017/ChatGpt
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules

#  mongo:
#    image: mongo:6.0
#    container_name: mongo
#    ports:
#      - "27017:27017"
#    volumes:
#      - mongo-data:/data/db
#
#volumes:
#  mongo-data:
