version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/wwebjs-bot
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    network_mode: host

volumes:
  mongo-data:
